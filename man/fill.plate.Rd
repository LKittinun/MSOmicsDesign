% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.plate.R
\name{fill.plate}
\alias{fill.plate}
\title{Fill plates with sample sequence.}
\usage{
fill.plate(
  samples = NULL,
  labels = NULL,
  nrows = 6,
  ncols = 9,
  row_letters = TRUE,
  plate_names = c("R", "G", "B", "Y"),
  addQC = FALSE,
  addQC.args = list(),
  exclude = NULL,
  exclude_label = "iQC"
)
}
\arguments{
\item{samples}{A vector containing sample names}

\item{labels}{Optional, additional labels matrices.}

\item{nrows}{Number of rows in a plate}

\item{ncols}{Number of columns in a plate}

\item{row_letters}{If \code{TRUE} will display \code{rownames} as alphabets}

\item{plate_names}{Vectors of plate names, will recycle if number of plates exceed names length}

\item{addQC}{If \code{TRUE} will addQC by function \code{addQC}}

\item{addQC.args}{Arguments for \code{addQC} functions}

\item{exclude}{Well positions to reserve on each plate. Either a character
vector of positions (e.g. \code{c("A1","B3")}) applied to every plate, or
a named list for per-plate control (e.g.
\code{list(all = "A1", R = "B3")}). The \code{"all"} list key applies to
every plate. Reserved wells are filled with \code{exclude_label} and
skipped when placing samples. Default \code{NULL} (no reserved wells).}

\item{exclude_label}{Label placed in reserved wells. Default \code{"iQC"}.}
}
\value{
\itemize{
\item \code{plate} A list of matrix containing samples filled in plates. If samples exceed a set of \code{plate_names} it will recycle into a new list.
\item \code{labels} A list of matrix containing their respective labels for samples. Only return if \code{labels} is not \code{NULL}
\item \code{data} An original sample sequence
}
}
\description{
This function will insert a vector of samples sequence in to plate matrix with specified rows and columns.
If number of samples exceeded a plate, the function will generate next plate automatically.
}
\examples{
samples <- LETTERS[1:20]
fill.plate(samples)

samples <- addQC(LETTERS[1:20], interval = 6)
fill.plate(samples)

## Can change plate names
fill.plate(samples, plate_names = c("B","Z","K","O"))

## When samples exceed a single plate (default 6 x 9 = 54 wells), additional
## plates are created automatically and named by recycling plate_names
big_samples <- paste0("S", 1:100)
result <- fill.plate(big_samples)
length(result$plate)   # 2 plates: "R" (54 samples) and "G" (46 samples)
result$plate[["R"]]    # first plate matrix
result$plate[["G"]]    # second plate matrix (partially filled)

## Reserve position A1 on every plate as instrument QC
fill.plate(paste0("S", 1:20), exclude = "A1")

## Per-plate: different exclusions on plate R vs plate G
fill.plate(paste0("S", 1:100),
           exclude = list(all = "A1", G = "B3"))
}
\seealso{
Other plate: 
\code{\link{addQC}()},
\code{\link{generate.position}()},
\code{\link{merge.plate_list}()},
\code{\link{plot.plate}()},
\code{\link{plot.plate_list}()},
\code{\link{print.plate_list}()},
\code{\link{write.sld}()}
}
\concept{plate}
