% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.plate.R
\name{merge.plate_list}
\alias{merge.plate_list}
\alias{merge.plate}
\alias{merge_plate}
\title{Merge two plate_list objects}
\usage{
\method{merge}{plate_list}(x, y, suffixes = c("_1", "_2"), ...)

\method{merge}{plate}(x, y, suffixes = c("_1", "_2"), ...)

merge_plate(x, y, suffixes = c("_1", "_2"), ...)
}
\arguments{
\item{x}{A \code{plate_list} object.}

\item{y}{A \code{plate_list} object.}

\item{suffixes}{Length-2 character vector of suffixes appended to conflicting
plate names. Default \code{c("_1", "_2")}.}

\item{...}{Currently unused.}
}
\value{
A \code{plate_list} with combined plates, labels (if present in both),
and data slots.
}
\description{
Combines two \code{plate_list} objects (e.g. from two batches or two calls to
\code{\link[=fill.plate]{fill.plate()}}) into a single \code{plate_list}. Plate names are deduplicated
by appending a batch suffix when there are conflicts.
}
\examples{
a <- fill.plate(paste0("S", 1:20))
b <- fill.plate(paste0("S", 21:40))
merged <- merge_plate(a, b)
length(merged$plate)

## Conflicting plate names get suffixes
c1 <- fill.plate(paste0("S", 1:60))
c2 <- fill.plate(paste0("S", 61:120))
merged2 <- merge_plate(c1, c2)
names(merged2$plate)
}
\seealso{
Other plate: 
\code{\link{addQC}()},
\code{\link{fill.plate}()},
\code{\link{generate.position}()},
\code{\link{plot.plate}()},
\code{\link{plot.plate_list}()},
\code{\link{print.plate_list}()},
\code{\link{write.sld}()}
}
\concept{plate}
