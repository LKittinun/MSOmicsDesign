% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block.rand.R
\name{block.rand}
\alias{block.rand}
\title{Block randomization from a given data.frame}
\usage{
block.rand(
  df,
  ...,
  block_size = NULL,
  n_blocks = NULL,
  min_block_size = 2,
  max_unique_prop = 0.5,
  seed = 1
)
}
\arguments{
\item{df}{A data frame, tibble, data.table, or any object coercible to a
data frame (e.g. a named matrix) containing sample identifiers and metadata.}

\item{...}{Unquoted column names to use as covariates. All unique
combinations of these columns define the groups used for block
randomization.}

\item{block_size}{Target number of samples per covariate group per block.
More samples per block means fewer, larger blocks. Default is \code{1} (one
sample per group per block, maximising the number of blocks). Specify
only one of \code{block_size} or \code{n_blocks}.}

\item{n_blocks}{Number of blocks to create. Directly sets the number of
blocks regardless of group size. Specify only one of \code{block_size} or
\code{n_blocks}.}

\item{min_block_size}{Minimum acceptable number of samples in any
covariate group. Groups with fewer members than this threshold trigger
a warning because their small size may lead to unreliable randomization.
Default is \code{2}. This check is advisory only and does not stop execution
unless the group is also too small to form 2 blocks (see \code{block_size}
and \code{n_blocks}).}

\item{max_unique_prop}{Maximum allowed ratio of unique covariate
combinations to total sample count. If
\verb{(number of unique groups) / (number of rows)} exceeds this value, an
error is thrown. This guards against situations where nearly every sample
is its own group, which makes block randomization meaningless. Default
is \code{0.5}.}

\item{seed}{Integer seed passed to \code{set.seed()} for reproducibility.
Set to \code{NULL} to skip seeding. Default is \code{1}.}
}
\value{
A data frame with class \code{"block.rand"} containing all original columns plus:
\itemize{
\item \strong{block.id} — zero-padded block number each sample was assigned to.
\item \strong{block.size} — number of samples in that block.
\item \strong{unique_group} — covariate combination label for each sample
(columns joined by \code{"_"}).
\item \strong{unique_group_id} — unique per-sample label within each covariate
group (e.g. \code{"ctrl_treat_1"}, \code{"ctrl_treat_2"}).
}
}
\description{
Assigns samples to randomized blocks while balancing across covariate groups.
One or more columns are used as covariates; every unique combination of
covariate values defines a group, and samples are distributed evenly across
blocks within each group. Internally uses \code{randomizr::block_ra()}.
}
\examples{
data(ccc)

# Default: maximise blocks (block_size = 1)
ccc_block <- block.rand(ccc, time, response)
class(ccc_block)
ccc_block

# Fewer, larger blocks: 2 samples per group per block
block.rand(ccc, time, response, block_size = 2)

# Directly specify the number of blocks
block.rand(ccc, time, response, n_blocks = 3)
}
\seealso{
Other randomize: 
\code{\link{check.block}()}
}
\concept{randomize}
