% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.plate_list.R
\name{plot.plate_list}
\alias{plot.plate_list}
\title{Plot a plate list}
\usage{
\method{plot}{plate_list}(
  x,
  labels = NULL,
  control.suffix = NULL,
  text = TRUE,
  return_df = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{plate_list} object returned by \code{fill.plate()}.}

\item{labels}{Optional. Either a flat character vector (split across plates
automatically) or a \code{plate}-class list returned by \code{fill.plate()$label}
(passed directly, one matrix per plate). If \code{NULL}, wells are labelled
with their sample values.}

\item{control.suffix}{Character vector of control prefixes (e.g. \code{"QC"}).
Strips trailing \verb{_<number>} suffixes so all QC replicates share one colour.}

\item{text}{If \code{TRUE} (default), sample names are printed inside each well.}

\item{return_df}{If \code{TRUE}, each list element is \code{list(plot, data.frame)}
instead of a plain ggplot object.}

\item{...}{Currently unused; included for S3 method compatibility.}
}
\value{
A list of \code{ggplot} objects, one per plate. If \code{return_df = TRUE},
each element is \code{list(plot, data.frame)}.
}
\description{
Plot a plate list
}
\examples{
samples <- addQC(LETTERS[1:20], interval = 6)
fp <- fill.plate(samples)

# No labels
plot(fp)

# Plate-structure labels from fill.plate()
fp2 <- fill.plate(samples, labels = paste0("lbl_", seq_along(samples)))
plot(fp2, labels = fp2$label)
}
\seealso{
Other plate: 
\code{\link{addQC}()},
\code{\link{fill.plate}()},
\code{\link{generate.position}()},
\code{\link{merge.plate_list}()},
\code{\link{plot.plate}()},
\code{\link{print.plate_list}()},
\code{\link{write.sld}()}
}
\concept{plate}
